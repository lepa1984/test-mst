const swiperMain = new Swiper('.main-swiper', {
    spaceBetween: 16,
    slidesPerView: 1,
    pagination: {
        el: '.main-swiper .swiper-pagination',
        type: 'bullets',
        clickable: true,
    },
});
